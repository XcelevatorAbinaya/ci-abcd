name: Use Secrets

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Use secret

        run: |

          echo "Username is ${{ vars.set }}"

          echo "Password length is ${#set}"

        env:

          set: ${{ vars.set}}
